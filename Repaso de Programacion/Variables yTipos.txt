#include <stdio.h>

int main() {
  int a = 3;
  float b = 4.5;
  double c = 5.25;
  float sum = a+b+c;

  /* La variable sunm no tenía colocada la suma */

  printf("The sum of a, b, and c is %f.", sum);
  return 0;
}